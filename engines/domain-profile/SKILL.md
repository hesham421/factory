---
name: domain-profile
description: Domain Profile — governed analysis stage, pass pre of the governance-factory. Run through `gov.py run-stage domain-profile`; never by hand.
---
<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->

# domain-profile — Domain Profile

| Contract | Value |
|---|---|
| Stage id | `domain-profile` |
| Kind | governed pipeline stage |
| Pass | pre · once per platform |
| Questions | **allowed** — resolved in-dialogue by the lane's implementers, never escalated to a file |
| Lane | `analysis` → claude:opus (effort high) |
| Inputs | `raw-idea`, `platform-brief?` — read from `_state/` |
| Produces | `domain-profile.md` |
| Owns IDs | — |
| Research | web — cite primary sources |
| Then | `P-1` |

## How this stage runs (orchestrator-owned — see `shared/GOVERNANCE-CORE.md` §6)
1. `gov.py state` refreshes `_state/`; the brief = `references/ENGINE.md` rendered with `profile` + `factory` + `stage` + the state files.
2. The lane dispatches the brief as a converging dialogue (4 rounds max, converge on *mutually-acceptable*).
3. Artifacts are written to the module version folder; `gov.py analyze` runs the contracts; the stage commits (`{stage}: [{MOD}] v{version} — {summary}`).
4. Questions are allowed here and closed inside the dialogue with a recommended, researched answer; nothing is written to an external open-questions file.
## Load order
`profiles/erp.yaml` (active profile) → `shared/ARTIFACT-CONTRACTS.md` → `shared/CONSTITUTION.md` → `shared/GOVERNANCE-CORE.md` → `shared/MARKER-PROTOCOL.md` → `shared/QUALITY-RUBRIC.md` → `shared/REGISTRY-SCHEMA.md` → `shared/VERSIONING.md` → `shared/XM-PROTOCOL.md` → `references/ENGINE.md`.
