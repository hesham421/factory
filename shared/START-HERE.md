<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->
# START HERE — the governance-factory in one page

**What it is.** An analysis factory: a raw idea goes in, reviewed and traceable analysis comes out — up to backend and frontend
execution plans delivered to the consumer repos. It **stops at delivery** (analysis-only). The domain is data
(`profiles/<id>.yaml`); the active one is `erp`.

**Where the truth lives.** `factory.yaml` (factory facts) + the profile (domain facts). Every table in the docs, every
`SKILL.md`, every command and this page are generated from them (`gov.py render`); `gov.py lint` refuses drift.

**How a module moves.**

1. `domain-profile` — Domain Profile *(questions allowed, closed in dialogue)*
2. `P-1` — Registry & Steering Builder
3. `P0` — Platform Inception *(questions allowed, closed in dialogue)*
4. `P0.5` — PRD *(questions allowed, closed in dialogue)* → **human-approval `prd-approval`**
5. `P1` — SRS
6. `P2` — Database
7. `P3.1` — Backend Execution Plan → **review `pass-1`**
8. `P3.2` — Frontend — UX Design + Execution Plan → **review `pass-2`**
Then split (marker protocol) → deliver → tag. Outside the line, on demand: `test-gen`, `api-verify`.

**What a human decides.** `prd-approval` (human-approval after `P0.5`); `pass-1` (review after `P3.1`); `pass-2` (review after `P3.2`). Nothing else asks.
After the last question-allowed stage, an engine that meets ambiguity writes an ADR (`decisions/<MOD>/`) and continues; a breaking one stops.

**Versions.** v1 is the module's base folder; vN holds only what changed plus a change manifest; `gov.py state` folds them
into `_state/`, the only thing engines read.

**Read next.** `shared/CONSTITUTION.md` → `shared/GOVERNANCE-CORE.md` → `shared/ARTIFACT-CONTRACTS.md` → `shared/MARKER-PROTOCOL.md` → `shared/VERSIONING.md` → `shared/QUALITY-RUBRIC.md`.
