# Prompt — `governance-shared`: scan first, then build

انسخ كل ما تحت الخط إلى محادثة جديدة.

---

## المهمة

نفّذ الخطوات ١–٦ من `factory/GOVERNANCE-SHARED-DESIGN.md` §٩: إنشاء ريبو
`governance-shared` مقسَّماً، وربطه submodule في المستودعات الثلاثة، وبناء
`gov.py sync`.

**المرحلة أ منتهية بالفعل** — لا تُعِدها. ما أُنجز:
- `factory.yaml → repos.backend.publishes.api-docs` = `governance/modules/{MOD}/api-docs` (مجلد)
- `factory.yaml → inputs.api-docs.merge` — قاعدة الطيّ (`lead` · `include` · `keep_alongside`)
- `gov.py::_merge_published()` — يطوي المجلد إلى ملف واحد **حتمياً** بترويسة تحمل digest
- `gov.py::cmd_fetch_inputs` — يتعامل مع المجلد ولا يلمس الملف المرافق
- `_inputs/api-docs-binding-sec.md` — ملحق `API ID BINDING` مفصول عن api-docs
- `fetch-inputs` يعمل فعلاً: SEC ١١ ملفاً · FIN ١٠ · MDL ٤

---

## أولاً: امسح قبل أن تبدأ (إلزامي — لا تكتب سطراً قبله)

**اقرأ هذين المستندين كاملين أولاً:**
- `factory/GOVERNANCE-SHARED-DESIGN.md` — التصميم (§١١ جرد الأدوات)
- `factory/SHARED-GOVERNANCE-PLAN.md` — الخلفية والقياسات

**ثم تحقّق بنفسك — لا تثق بالمستندين، فقد تكون الأرض تغيّرت:**

1. **حالة git للمستودعات الأربعة**
   `factory` · `backend` · `frontend` — الفروع، الـ remotes، هل هناك عمل غير ملتزَم؟
   (المجلد الجذر `~/my project` ليس مستودع git — لا تفترض غير ذلك.)

2. **هل `governance-shared` موجود على GitHub؟** لا تفترض. الحساب `hesham421`.

3. **أعد إجراء جرد §١١ بنفسك**، لا تنسخه:
   ```
   grep -rl "api-docs" --include="*.md" --include="*.py" \
     backend/.claude frontend/.claude backend/governance/governance-tools \
     factory/governance-tools factory/shared factory/engines factory/standalone
   ```
   المستند يقول ٣١ ملفاً · ٤ تتغيّر. **تحقّق من الرقمين.**

4. **أكّد الحقيقتين اللتين تجعلان الجرد صغيراً:**
   - أوامر الموديولات **مولَّدة** (`generate-module-setup.md` ينتج
     `.claude/commands/{MOD}/execute-backend.md`) — فلا تُحرَّر يدوياً
   - محرّكات المصنع **لا تحوي مساراً حرفياً** — كلها ترندر من الإعدادات
   لو انكسرت إحداهما، **توقّف واسأل**: الخطة كلها مبنية عليهما.

5. **شغّل خط الأساس وسجّله:**
   ```
   cd factory && .venv/bin/python governance-tools/gov.py lint
   .venv/bin/python -m pytest governance-tools/tests -q
   ```
   المتوقّع: `0/0/0` · **237 ناجح، فشل واحد** —
   `test_markers.py::test_threshold_of_non_marker_kind_is_not_counted`.
   **هذا الفشل قديم وغير ذي صلة.** أي فشل آخر = الأرض تغيّرت؛ توقّف.

6. **تأكّد أن `fetch-inputs` ما زال يعمل** (المرحلة أ لم تُنقَض):
   ```
   .venv/bin/python governance-tools/gov.py fetch-inputs -m SEC -v 1
   ```
   المتوقّع: `unchanged api-docs …` + `kept api-docs-binding-sec.md`.

**اعرض ما وجدته قبل التنفيذ.** لو خالف المستند الواقع، **الواقع هو الحَكَم** —
قل ذلك ولا تُصلح المستند بصمت.

---

## ثانياً: نفّذ بهذا الترتيب

| # | الخطوة | معيار الإنجاز |
|---|---|---|
| ١ | أنشئ `governance-shared` بالتقسيم + `CODEOWNERS` (§٢، §٣) | الهيكل قائم، الملكية مفروضة |
| ٢ | انقل api-docs الستّة **بتاريخها** (`git filter-repo` أو `subtree`) | لا يضيع تاريخ |
| ٣ | اربط submodule في الثلاثة (§٤) | `clone --recursive` يعمل |
| ٤ | **`gov.py sync`** + `--dry-run` (§٥-٥) | **حرج — اقرأ أدناه** |
| ٥ | أعد توجيه `generate-api-docs` + المولِّدَين (§١١-١) | الكتابة المباشرة |
| ٦ | فحوص §٦ + CI | منع التراجع |

### الخطوة ٤ ليست اختيارية

قِيس التردّد: **~٣ مزامنات أسبوعياً**. بلا `gov.py sync` تصبح كل واحدة
٣ commits + ٣ ترقيات مؤشّر يدوية = **٩ commits أسبوعياً من الطقوس** —
أثقل من النسخ اليدوي الذي نلغيه.

> **لو تعذّرت الخطوة ٤، توقّف واسأل. التصميم بلا `sync` تراجع لا تقدّم.**

---

## قواعد العمل (مستخلَصة من جلسات سابقة على هذا المصنع)

1. **لا حرفيات في الكود** (C1). المسارات والأسماء في `factory.yaml` أو البروفايل.
   هذه القاعدة وحدها أنقذت ١٢ ملفاً من التعديل.

2. **صفّ فحص بلا سند يُحذف لا يُلطَّف.** لو حذفت محتوى، احذف الفحص الذي
   يؤكّده — وقل ذلك صراحةً.

3. **الاشتقاق يجب أن يكون حتمياً.** نفس المصدر = نفس البايتات. `_merge_published`
   و `build_state` كلاهما يحقّقها؛ أي جديد يجب أن يحقّقها.

4. **`_state/` مشتقّة ومُتجاهَلة في git** (`current-*.md` · `traceability.md` ·
   `state.json`). `approvals/` · `gate-*` · `analyze-*` · `briefs/` **مُتتبَّعة** —
   لا تتجاهلها.

5. **الفحص الذي لا يستطيع الرؤية يجب أن يقولها**، لا أن يختلق نتائج.
   سابقة: `C7.23` أعطى ١٤ بلاغاً كاذباً في SEC؛ الصواب بلاغ واحد
   («٢٠ من ٢٧ بلوك لا تحمل `ENT`»).

6. **الاختبارات الحارسة تمسك الأخطاء — عدّلها، لا تتجاوزها.** ثلاثة اختبارات
   أمسكت أخطاءً حقيقية أثناء `C7.23`.

7. **لا تنسخ ملفاً بين المستودعات يدوياً.** لو احتجت ذلك، فالآلية ناقصة.

8. **تحقّق بالتشغيل لا بالقراءة.** كل ادعاء في هذا المستند أُثبت بأمر.

---

## ما لا تفعله

| | لماذا |
|---|---|
| مجلد مشترك خارج المستودعات | يكسر الاكتفاء الذاتي المنصوص عليه في `factory.yaml` |
| السماح للفرونت بالكتابة في api-docs | مشتقّة من الكود — التعديل يُمحى عند أول توليد |
| نسخة ثانية من api-docs في `frontend/` | العزل يمنع الكتابة لا القراءة؛ النسختان هما المشكلة |
| symlinks أو git subtree | لا تنجو من `clone` · Windows · CI |
| `push` بلا `--dry-run` أولاً | `sync` يكتب داخل مستودعات أخرى |
| إعادة المرحلة أ | منتهية ومُتحقَّق منها |

---

## سياق يوفّر عليك وقتاً

- **الباك مُنفَّذ لكل الموديولات الحالية** (SEC · FIN · MDL · CU · FILE · NOTIF).
  التعديلات تنفع الموديولات اللاحقة؛ الحالية تلتحق عند أول إعادة توليد.
- **CU · FILE · NOTIF** لها api-docs في الباك ولا نسخة في الفرونت — الريبو
  المشترك يحلّها بنيوياً (مصدر واحد).
- **SEC · FIN · MDL**: نسختا الباك والفرونت متطابقتان اليوم (٠ انحراف).
- **البيئة:** `factory/.venv/bin/python` (jinja2 + yaml). لا تستخدم `python3` المجرّد.
- **عمل غير ملتزَم في `factory`** من جلسات سابقة (تعديلات المحرّكات · `C7.23` ·
  `.gitignore` · المرحلة أ). راجعه قبل أن تلتزم أي شيء جديد.
