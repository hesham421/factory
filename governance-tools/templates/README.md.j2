{{ marker }}
# {{ factory.factory.name }} — the Analysis Factory (v{{ factory.schema_version }})

A git-native, **domain-agnostic analysis factory**. Input: a raw product idea + a domain profile.
Output: reviewed, versioned, traceable analysis artifacts and **backend + frontend execution
plans**, written into the shared governance repo every consumer reads. Boundary:
**{{ factory.factory.boundary }}** — it
never implements, never audits implementations inside the line, never runs tests inside the line.

Everything below is generated from `factory.yaml` + `profiles/{{ profile.identity.id }}.yaml` by `gov.py render`.
The constitution that governs the repo is `shared/CONSTITUTION.md`.

## Layout
```
factory.yaml                 single source of truth: stages, passes, gates, lanes, paths, naming, ID grammar, marker grammar
{{ factory.paths.profiles }}/                    a domain is DATA — _schema.yaml + <id>.yaml (+ <id>/knowledge/*)
{{ factory.paths.domain }}/                      domain-profile.md (entry gate, from the conversational domain-profile stage) + project-registry.md (P-1 output)
{{ factory.paths.engines }}/<id>/                the governed pipeline: SKILL.md (generated) + references/ENGINE.md (Jinja2 template)
{{ factory.paths.standalone }}/<id>/             outside the line, on demand, never a gate
{{ factory.paths.shared }}/                      the core doc set (tables generated in place)
{{ factory.paths.reviewers }}/pass-review.md     the single gate template
{{ factory.paths.decisions }}/<MOD>/             ADR stream
{{ factory.paths.modules }}/<MOD>/[vN/]          v1 = base folder, vN = delta only; {{ factory.paths.module.state_dir }}/ generated current state; {{ factory.paths.module.inputs_dir }}/ fetched inputs; {{ factory.paths.module.packages_dir }}/ split output
{{ factory.paths.commands }}/                    thin command wrappers (generated)
{{ factory.paths.tools }}/            config.py (loader) · gov.py (orchestrator) · toolkit/ (one parser/splitter/structure/archive) · tests/
history/                     pre-v6 artefacts, loaded by nothing
```

## The pipeline
<!-- RENDER:stages -->
<!-- /RENDER:stages -->

Passes: {% for k, p in factory.passes.items() %}**pass {{ k }}** = {{ p.stages | join(' → ') }} → {{ p.then | join(' → ') }} (track `{{ p.track }}`{% if p.required_inputs %}, requires {{ p.required_inputs | join(', ') }} fetched back{% endif %}){% if not loop.last %}; {% endif %}{% endfor %}.
{% for k, p in factory.passes.items() if p.session == 'bundled' %}{% if loop.first %}All stages of a pass run in one delegate session with one commit per stage.{% endif %}{% endfor %}

### Standalone stages (outside the line)
<!-- RENDER:standalone -->
<!-- /RENDER:standalone -->

### Human decision points
<!-- RENDER:gates -->
<!-- /RENDER:gates -->

### Lanes (model/effort per reasoning step)
<!-- RENDER:lanes -->
<!-- /RENDER:lanes -->

## Active profile
<!-- RENDER:profile-summary -->
<!-- /RENDER:profile-summary -->

Add a domain: `gov.py new-domain <id>` scaffolds `{{ factory.paths.profiles }}/<id>.yaml` from `_schema.yaml`; `gov.py lint --profile <id>` validates it; no engine text changes.

## Commands
<!-- RENDER:commands -->
<!-- /RENDER:commands -->

## Operating a module (the orchestrator owns the protocol — `gov.py`, exit 0 ok · 1 blocked · 2 awaiting the operator)
```
gov.py run-stage domain-profile -m MOD      # writes the brief; run it as the conversational stage, save {{ factory.paths.domain }}/domain-profile.md
gov.py run-stage P-1 -m MOD [--complete]     # bootstrap once per platform → {{ factory.paths.platform }}/project-registry.md
gov.py run-pass 1 -m MOD [--new]             # ONE bundled brief for the whole pass (stages run in one delegate session)
gov.py run-stage <id> -m MOD -v N --complete # after each stage's files exist: analyze → commit (stops on CRITICAL / [QUESTION] / BLOCKED ADR)
gov.py approve prd-approval -m MOD -v N      # the human decision after P0.5 — nothing asks a question after this
gov.py gate 1 -m MOD -v N                    # analyze must be CLEAN → reviewer brief; then --complete --result review.json
gov.py split --track backend -m MOD -v N     # marker protocol → packages (+ SHA verification)
gov.py sync                                  # where the shared repo stands, and who is behind it
gov.py fetch-inputs -m MOD -v N              # pass-2 hard gate: api-docs, written by the backend
gov.py run-pass 2 … · gate 2 … · split --track frontend … · gov.py tag -m MOD -v N
gov.py run-standalone api-verify -m MOD -v N             # outside the line, on demand
gov.py run-standalone test-gen --module MOD (or --modules A,B,... or --scope project) -v N   # test-gen's own scopes
gov.py version -m MOD --new                  # a delta version: only what changed + change-manifest; gov.py state folds it
```
`gov.py next -m MOD [-v N] [--run]` prints — or with `--run` executes — the single next protocol step, derived from what exists (artifacts, approvals, gate records, packages, tags).

## Runners — how a brief reaches a model
Every reasoning step (a stage, a gate review, a revise) is a brief `gov.py` builds; `{{ factory.runner.env }}` chooses who answers it:
- `cmd` — `${{ factory.runner.cmd_env }}` is run once per round with `{% for p in factory.runner.placeholders %}{{ '{' ~ p ~ '}' }}{{ ' ' if not loop.last }}{% endfor %}` filled; `{% for p in factory.runner.required %}{{ '{' ~ p ~ '}' }}{{ ' ' if not loop.last }}{% endfor %}` is mandatory in the template (a dialogue lane alternates implementers, and a template that does not pass the model collapses the debate to one). The shipped template is the adapter `{{ factory.runner.adapter }} {{ factory.runner.adapter_args }}`, which runs the claude-delegate relay (`{{ factory.runner.relay }}`, or `${{ factory.runner.relay_env }}`) and hands the implementer's final response back as the `{out}` file. In this mode **nothing waits for an operator**: `run-pass` advances stage by stage (dispatch → analyze → commit), `gate` dispatches the review lane and records the verdict, a REVISE is applied by the gate's `on_revise` lane, re-analyzed and re-gated at most {{ factory.review.revise_max }} time(s), then ESCALATES to a human. What still stops it: a `[QUESTION]` from a questions-forbidden stage, a BLOCKED ADR, a finding at {{ factory.analyze.blocking | join('/') }}, and the human decision points ({% for g in factory.gates if g.type == 'human-approval' %}`{{ g.id }}`{{ ', ' if not loop.last }}{% endfor %}).
- `manual` — the brief is written, the command exits 2, and the operator (Claude Code + the delegate skills) executes it by hand and calls the command again with `--complete`; a pass is one bundled brief.
- `fake` — the test hook (the nightly job drives the whole line through it under a non-ERP toy profile).

## Quality gates in code
- `gov.py analyze` — every machine-checkable clause of `shared/ARTIFACT-CONTRACTS.md` (EARS, traces, orphans, ID ownership/continuity, registry agreement, markers, manifest); a gate cannot open with a CRITICAL.
- `gov.py lint` — the constitution: profile schema, no removed concepts, no domain literals outside profiles, no stage/phase/prefix literals in code, generated files fresh.
- `python3 -m pytest {{ factory.paths.tools }}/tests -q` — the toolkit and orchestrator tests (the checked-in `{{ profile.identity.id }}` profile + a synthetic toy profile, proving domain-agnosticism).

## Linking consumer repos
Edit `factory.yaml → repos` (url, checkout, publishes). Nothing is copied between repos: governance is written once into `{{ factory.repos.shared.url }}`, which all three mount. `paths.external` names the keys that resolve there — `{{ factory.paths.external['keys'] | join(', ') }}` — so every consumer reads the artifact where it was written. The backend writes api-docs to `{{ factory.repos.backend.publishes['api-docs'] }}` inside it; releases are tagged `{{ factory.naming.tag }}`.

Each `repos.<name>` resolves through `CFG.repo_checkout()`: the env var named by `checkout_env`, falling back to `checkout_default` (a path relative to this factory's own root). That one mechanism supports two deployments with **zero code difference**, only config values:
- **Standalone** — this factory is its own repo; `checkout_default` points at a sibling checkout (e.g. `../backend`).
- **Embedded** — this factory lives inside `<consumer-repo>/governance/`; `checkout_default: ".."` resolves straight to that repo's own root.

Nothing in `governance-tools/` assumes either shape — don't special-case one when adding a repo link.
