# خطة المستودع المشترك للحوكمة (`governance-shared`)

> **الهدف:** إلغاء النقل اليدوي للملفات بين `factory` و `backend` و `frontend`،
> وجعل التصحيح في مكان واحد يراه الجميع.
>
> الحالة: **مقترح — لم يُنفَّذ شيء منه بعد.**

---

## ١ · ما وجدته بالفحص (الأساس الواقعي للخطة)

### ١-١ عيب حقيقي: مسار النشر في `factory.yaml` لا وجود له

```yaml
repos.backend.publishes:
  api-docs: "governance/api-docs/api-docs-{mod}.md"     # ← ملف واحد
```

الواقع في الباك إند:

```
backend/governance/modules/{MOD}/api-docs/
    index.md
    endpoints/*.md                                      # ← مجلد بملفات كثيرة
```

`backend/governance/api-docs/` **غير موجود إطلاقاً**.

**الأثر:** `gov.py fetch-inputs` لا يمكن أن ينجح — يبحث عن مسار لا وجود له،
فيُرجع `GATE CLOSED — missing inputs`. الشكل مختلف أيضاً: ملف واحد مقابل مجلد.

### ١-٢ نتيجة العيب: الدمج يدوي

رأس `factory/erp/modules/SEC/_inputs/api-docs-sec.md`:

```
> Source: `erp/modules/SEC/api-docs/index.md` + `endpoints/*.md`,
> concatenated for pass-2 input.

## API ID BINDING — added by P3.2 reconciliation (ADR-SEC-004)
```

هذا الملف (٢٣٥١ سطراً) **صُنع يدوياً**: دمج + ملحق ربط `API-*` مكتوب بخط اليد.
لا أداة تنتجه، ولا أداة تعيد إنتاجه. هذا بالضبط «الضغط» الذي تريد تخفيفه.

### ١-٣ الازدواج قائم لكنه لم يتفجّر بعد

| الموديول | الباك | الفرونت | الانحراف |
|---|---|---|---|
| SEC · FIN · MDL | ✅ | ✅ | **٠ اختلاف** |
| CU · FILE · NOTIF | ✅ | ❌ | الفرونت لا يملك نسخة |

نسختان متطابقتان اليوم — لكن لا شيء يمنع انحرافهما غداً، ولا شيء يكتشفه.

### ١-٤ آلية التوزيع القائمة تعمل فعلاً

| الاتجاه | الأداة | الحالة |
|---|---|---|
| المصنع ← الباك/الفرونت (packages) | `gov.py deliver --track` | ✅ يعمل |
| المصنع ← الجميع (modules-registry) | `gov.py publish` | ✅ يعمل |
| الباك ← المصنع (api-docs) | `gov.py fetch-inputs` | ❌ **معطّل** (المسار) |

**استنتاج مهم:** البنية موجودة بالكامل. العطل في اتجاه واحد فقط.

---

## ٢ · تقييمي للمقترح

### ما هو صحيح فيه

- **تشخيص الألم دقيق.** النقل اليدوي حقيقي وموثَّق في رأس الملف نفسه.
- **«التصحيح في مكان واحد» هو المبدأ الصحيح** — نفس مبدأ «سلطة واحدة لكل مفهوم»
  الذي طبّقناه على المحرّكات والمهارات.

### ما أعترض عليه

**١. «مكان مشترك يقرأ منه الجميع» يكسر قاعدة معمارية قائمة في `factory.yaml`:**

> *"A consumer repo must be self-sufficient: everything it reads lives inside its
> own checkout, never at a path above it… no consumer reaches into another repo's
> tree to read it."*

مجلد مشترك خارج المستودعات يعني:
- الباك لا يُبنى وحده في CI (يحتاج مجلداً أعلى منه)
- لا يمكن استنساخ الفرونت منفرداً
- تحديد المسؤولية يضيع: من يملك الملف؟

**٢. «أي مشروع يكتشف خطأً يعدّل في المكان المشترك» يعكس اتجاه السلطة.**
الـ api-docs تُولَّد من **الباك الشغّال** (`/v3/api-docs`). لو عدّلها الفرونت
يدوياً، تصبح كذبة تُمحى عند أول `generate-api-docs`. الخطأ في الـ api-docs
**عيب في الكود**، لا في الوثيقة.

### الحكم

**المشكلة صحيحة · الحل المقترح خطير.**
البديل يحقق نفس الهدف بلا كسر: **مصدر واحد + توزيع آلي**، لا مجلد مشترك.

```
مصدر واحد (الباك يولّده)  →  المصنع يسحب ويحوّل  →  يوزّع على الجميع
```

لا أحد يقرأ من خارج مستودعه. لا أحد يعدّل يدوياً. التصحيح يتم في المصدر
ويصل للجميع بأمر واحد.

---

## ٣ · الخطة

### المرحلة أ — إصلاح العطل (تُلغي معظم العمل اليدوي وحدها)

#### أ-١ تصحيح مسار النشر

`factory.yaml`:
```yaml
repos.backend.publishes:
  api-docs: "governance/modules/{MOD}/api-docs/"        # مجلد لا ملف
```

#### أ-٢ تعليم `fetch-inputs` دمج المجلد

`cmd_fetch_inputs` اليوم يفعل `shutil.copy2` لملف واحد. يحتاج:
لو كان المصدر مجلداً → ادمج `index.md` + `endpoints/*.md` بترتيب ثابت
(أبجدي) إلى الملف الواحد الذي يقرؤه المصنع.

**شرط:** الدمج **اشتقاق بحت** — بصمة المصدر تُسجَّل في رأس الناتج،
وإعادة التشغيل تُنتج نفس البايتات. (نفس مبدأ `build_state` الذي تحقّقنا منه.)

#### أ-٣ ملحق ربط `API-*` — لا يُدمج يدوياً

الملحق المكتوب بخط اليد (`API ID BINDING`) ليس جزءاً من الـ api-docs.
مكانه الصحيح: `_inputs/api-docs-binding-{mod}.md` منفصلاً، يُقرأ بجانبه.
هكذا لا يُمحى عند إعادة السحب.

> **بعد المرحلة أ:** لا دمج يدوي، ولا ملحق ضائع. هذا وحده يحلّ ~٨٠٪ من الألم.

---

### المرحلة ب — توزيع الـ api-docs على الفرونت

#### ب-١ تسجيل الـ api-docs كـ «منشور»

```yaml
publications:
  api-docs:
    derived_from: repos.backend.publishes.api-docs

repos.frontend.receives:
  api-docs: "governance/modules/{MOD}/api-docs/"
```

#### ب-٢ توسيع `gov.py publish`

`publish` يوزّع اليوم `modules-registry.json` فقط. يمتدّ ليوزّع الـ api-docs
من الباك إلى الفرونت — بنفس قواعد `preserve`/`additive` القائمة.

**النتيجة:** `gov.py publish api-docs` يزامن الفرونت. لا نسخ يدوي.

#### ب-٣ فحص انحراف

بند عقد جديد: نسخة الفرونت من api-docs تطابق نسخة الباك.
انحراف = `MAJOR`. اليوم ٠ اختلاف — والفحص يبقيها كذلك.

---

### المرحلة ج — المحتوى المشترك الحقيقي

هناك محتوى **يملكه المصنع** ويقرؤه الجميع (قواعد الحوكمة، تعريفات الذرّات،
سجل الموديولات). موجود أصلاً: `backend/governance/governance-shared/`
(يحوي `README.md` فقط اليوم).

- المصنع هو المالك الوحيد
- `gov.py publish` يكتبه داخل **كل** مستودع (نسخة محلية، اكتفاء ذاتي محفوظ)
- لا أحد يعدّله يدوياً — التعديل في المصنع ثم `publish`

> هذا يحقق «مكان مشترك» بالمعنى المنطقي، بلا مجلد فيزيائي مشترك.

---

## ٤ · الأماكن والأدوات المتأثرة

### تتغيّر

| الملف | التغيير | الخطورة |
|---|---|---|
| `factory/factory.yaml` | `publishes` · `publications` · `receives` | منخفضة |
| `factory/governance-tools/gov.py` | `cmd_fetch_inputs` (دمج مجلد) · `cmd_publish` | **متوسطة** |
| `factory/shared/ARTIFACT-CONTRACTS.md` | بند انحراف api-docs | منخفضة |
| `backend/.claude/commands/generate-api-docs.md` | يذكر أن المصنع يسحب منه | منخفضة |

### لا تتغيّر (مهم)

| الملف | السبب |
|---|---|
| `backend/…/generate-module-setup.md` | ينشئ هيكل الموديول محلياً — لا علاقة له بالنقل |
| `frontend/…/generate-frontend-module-setup.md` | نفس السبب |
| `backend/…/orchestrate-module.md` | يقرأ من `governance/modules/{MOD}/` ولا يتغيّر مساره |
| `frontend/…/orchestrate-module.md` | يقرأ `api-docs/` محلياً — يبقى كما هو، لكن محتواه يُزامَن آلياً |

> `orchestrate-module.md` في الفرونت يقول: *"api-docs is ground truth… the
> frontend never consults backend source"*. الخطة **تدعم** هذه القاعدة ولا تخالفها.

### أدوات لا تتأثر
`generate.py` (مولّد api-docs) · `api-verify` · `test-gen` · `deliver` · `analyze`

---

## ٥ · الترتيب والمخاطر

| # | الخطوة | الأثر | المخاطرة |
|---|---|---|---|
| ١ | أ-١ تصحيح المسار | `fetch-inputs` يعمل | لا شيء |
| ٢ | أ-٢ دمج المجلد | إلغاء الدمج اليدوي | **متوسطة** — الترتيب يجب أن يكون حتمياً |
| ٣ | أ-٣ فصل الملحق | لا يضيع عند السحب | منخفضة |
| ٤ | ب-١/ب-٢ التوزيع | إلغاء نسخ الفرونت اليدوي | **متوسطة** — كتابة داخل مستودع آخر |
| ٥ | ب-٣ فحص الانحراف | يمنع عودة المشكلة | منخفضة |
| ٦ | ج المحتوى المشترك | مصدر واحد للقواعد | منخفضة |

**المرحلة أ وحدها تستحق التنفيذ فوراً** — عطل مؤكَّد ذو حل صغير.

### مخاطر يجب الإقرار بها

1. **`publish` يكتب داخل مستودعات أخرى.** موجود اليوم لكن بنطاق أوسع.
   يجب `--dry-run` أولاً دائماً.
2. **الدمج يجب أن يكون حتمياً.** ترتيب غير مستقر = ضوضاء diff في كل سحب.
3. **`v3/api-docs` يجب أن يكون شغّالاً** لإعادة التوليد. لا يغيّره هذا المقترح
   لكنه يبقى نقطة اعتماد.
4. **الموديولات الثلاثة الحالية مُنفَّذة** — الخطة تنفع اللاحقة، وتُطبَّق على
   الحالية عند أول إعادة توليد.

---

## ٦ · ما لا أوصي به إطلاقاً

| المقترح | لماذا لا |
|---|---|
| مجلد فيزيائي خارج المستودعات الثلاثة | يكسر الاكتفاء الذاتي المنصوص عليه · يعطّل CI · يفقد المسؤولية |
| الفرونت يعدّل api-docs عند اكتشاف خطأ | الـ api-docs مشتقّة من الكود — التعديل يُمحى عند أول توليد. الخطأ يُصلَح في الكود |
| symlinks بين المستودعات | لا تنجو من `clone` ولا تعمل على Windows ولا في CI |
| git submodule للمشترك | يضيف طبقة تزامن أثقل من المشكلة الأصلية |

---

## ٧ · المتغيّر الثاني: **ريبو مشترك** (لا مجلد مشترك)

> أُضيف بعد توضيحك: ريبو git رابع يراه الثلاثة، كلٌّ يرى ما يخصّه.

### ٧-١ هذا مقترح مختلف — وأفضل

الثلاثة مستودعات حقيقية على نفس الحساب:

```
factory   → github.com/hesham421/factory
backend   → github.com/hesham421/backend
frontend  → github.com/hesham421/newmaxdashboard-implement_security
```

فريبو رابع ممكن تقنياً. والأهم: **يحلّ ٣ من اعتراضاتي الـ٤ على المجلد المشترك**:

| الاعتراض | مجلد مشترك | **ريبو مشترك (submodule)** |
|---|---|---|
| كسر الاكتفاء الذاتي | ❌ يكسره | ✅ **محلول** — الـ submodule داخل الـ checkout |
| تعطّل CI | ❌ يتعطّل | ✅ **محلول** — `clone --recursive` |
| ضياع المسؤولية | ❌ يضيع | ✅ **أفضل** — `CODEOWNERS` لكل مسار |
| الفرونت يعدّل api-docs | ❌ | ❌ **باقٍ** — عيب معماري لا مكانيّ |

**إذن: فكرتك الثانية أقوى من الأولى.** سحبتُ اعتراضي المعماري.

### ٧-٢ لكن القياس يكشف مفاجأة

عدّدت تحرّكات api-docs الفعلية:

```
الباك   : ١٤ commit على ٥ أيام (٥ سبتمبر → ١٣)
الفرونت : نفس الأيام بالضبط — ٦، ١١، ١٢، ١٣
```

| اليوم | الباك | الفرونت |
|---|---|---|
| 09-06 | ٢ | ٣ |
| 09-11 | ٦ | ١ |
| 09-12 | ٢ | ٢ |
| 09-13 | ١ | ١ |

تطابق الأيام **دليل قاطع** على النسخ اليدوي. لكنه يكشف التردّد أيضاً:
**~٣ مرات أسبوعياً**، لا يومياً.

### ٧-٣ التكلفة الحقيقية للـ submodule

الهدف المعلن: *«تخفيف الضغط الذي يخص نقل الملفات»*.

| | اليوم | مع submodule |
|---|---|---|
| توليد api-docs | commit في الباك | commit في **shared** |
| وصولها للمصنع | نسخ يدوي | `submodule update` + commit مؤشّر |
| وصولها للفرونت | نسخ يدوي | `submodule update` + commit مؤشّر |
| **الإجمالي/مزامنة** | نسختان | **٣ commits + ٣ تحديثات مؤشّر** |

**النتيجة المرّة:** الـ submodule **لا يقلّل الطقوس — بل ينقلها**.
نسخة يدوية ← مؤشّر submodule. وبتردّد ٣×/أسبوع، الفرق ليس في صالحه.

يضيف أيضاً:
- كل مساهم يجب أن يتذكّر `--recursive` و `submodule update`
- كاتبان (المصنع + الباك) على ريبو واحد → تعارضات دمج
- `clone` ناسٍ للـ `--recursive` = مستودع صامت الكسر

### ٧-٤ «كلٌّ يرى ما يخصّه» — لا يمكن فرضه في git

الـ submodule يعطي الجميع **كل شيء**. `sparse-checkout` ممكن لكنه هشّ
ولا يُفرَض على أحد. فـ«ما يخصّه» يبقى **عُرفاً** لا قاعدة — وهو ما يتحقّق
اليوم بالمجلدات أصلاً.

### ٧-٥ ما لا يحلّه الريبو المشترك إطلاقاً

**العطل الأساسي يبقى كما هو:**

```yaml
publishes.api-docs: "governance/api-docs/api-docs-{mod}.md"   # مسار غير موجود
الواقع            : governance/modules/{MOD}/api-docs/         # مجلد
```

نقل الملفات إلى ريبو مشترك **لا يصلح هذا**. ستظلّ تحتاج:
- تصحيح المسار (أ-١)
- دمج المجلد → ملف (أ-٢)
- فصل ملحق `API ID BINDING` (أ-٣)

وهي **نفس المرحلة أ** — بغضّ النظر عن مكان الملفات.

### ٧-٦ توصيتي

**نفّذ المرحلة أ أولاً.** ثم قِس:

- لو صار التردّد يومياً أو دخل مطوّر ثالث → **الريبو المشترك يستحقّ**
- لو بقي ٣×/أسبوع → `gov.py publish api-docs` (المرحلة ب) يحقّق نفس النتيجة
  بأمر واحد، بلا ريبو رابع ولا submodule

**السبب:** المرحلة أ شرط لازم في الحالتين، ورخيصة، وتكشف إن كان الباقي ضرورياً.

### ٧-٧ لو قرّرت الريبو المشترك رغم ذلك — التصميم

```
governance-shared/                 ← ريبو رابع، submodule في الثلاثة
  rules/          ← يكتبه المصنع فقط    (قواعد، ذرّات، عقود)
  registry/       ← يكتبه المصنع فقط    (modules-registry.json)
  api-docs/{MOD}/ ← يكتبه الباك فقط     (مولَّد من التطبيق الشغّال)
  packages/{MOD}/ ← يكتبه المصنع فقط    (المسلَّمات)
```

**قواعد غير قابلة للتفاوض:**
1. `CODEOWNERS` يفرض الكاتب الوحيد لكل مسار
2. **لا أحد يعدّل `api-docs/` يدوياً** — مشتقّة من الكود؛ الخطأ يُصلَح في الكود
3. المؤشّر يُثبَّت بوعي: نسخة v1 المجمّدة تبقى على commit-ها
4. CI في الثلاثة يفشل إن لم يكن الـ submodule محدَّثاً
