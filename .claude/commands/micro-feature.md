<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->
# /micro-feature — `gov.py version --new + run-pass 1 + run-pass 2 (delta)`

```
/micro-feature MOD "<free text>"
```

Thin wrapper: runs `python3 governance-tools/gov.py version --new + run-pass 1 + run-pass 2 (delta) MOD "<free text>"` from the repo root and prints its report. Behaviour, gates and lanes are defined in `factory.yaml` — nothing here.
