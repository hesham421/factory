<!-- GENERATED by `gov.py render` from factory.yaml + profile — do not edit by hand -->
# /reanalyze — `gov.py analyze --all-modules`

```
/reanalyze [--scope all|stage:ID|pass:N|gate:ID]
```

Thin wrapper: runs `python3 governance-tools/gov.py analyze --all-modules [--scope all|stage:ID|pass:N|gate:ID]` from the repo root and prints its report. Behaviour, gates and lanes are defined in `factory.yaml` — nothing here.
